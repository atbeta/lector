Windows 安装器在 `dist-win/` 下,以 `Lector_<版本>_x64-setup.exe` 命名,静默安装用 `/S`。
便携版 `lector-portable.zip` 双击 `register-file-assoc.cmd` 建立文件关联,`unregister-file-assoc.cmd` 解除。
`SHA256SUMS.txt` 提供校验和。

macOS 版暂无(签名与公证未做)。

---

## v0.27.7 (2026-09-17)

本版在 reading-polish 分支发布,未合回 main。其 commits 已并入后续版本。

### 新增

- enhance outline twisty accessibility and remove tooltips

### 修复

- repair 118 mojibake spots (U+FFFD) across comments and recover-unsaved hint
- open docs in dropped-on window, icon-only hover overlay, clear hint on drop
- suppress grid transition during boot to kill widen animation on open

### 改进

- smooth open/close - delayed hide on close, fixed-width content during track transition
