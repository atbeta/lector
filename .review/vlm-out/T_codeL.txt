(1) Distinct text colours and which words have them:

- Blue: “export”, “function”, “const”, “return”
- Black: All other words — “parseBlocks(text: string): BlockView[] {”, “tree = fromMarkdown(text, { extensions, mdastExtensions })”, “sliceSeamless(tree, text)”, “}”

(2) The highlighting is sparse — only a few keywords (“export”, “function”, “const”, “return”) are coloured blue; the rest of the code is in black.

(3) Yes, the box ends well before the right edge of the surrounding text column — there is significant white space to the right of the closing brace “}”.

(4) No clipping, cut-off characters, or misalignment visible — all characters are fully rendered and aligned within the rounded rectangle border.
