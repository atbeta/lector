{
  "name": "lector",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "tauri:dev": "tauri --config clients/tauri/src-tauri/tauri.conf.json dev --no-watch",
    "tauri:build": "tauri --config clients/tauri/src-tauri/tauri.conf.json build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "@types/bun": "1.4.0",
    "typescript": "^5.9.0"
  }
}
